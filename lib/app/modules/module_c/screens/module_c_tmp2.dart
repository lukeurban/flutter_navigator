import 'package:flutter/material.dart';

class ModuleCTmp2 extends StatelessWidget {
  static String route = '/C/tmp2';
  const ModuleCTmp2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ModuleCTmp2'),
      ),
    );
  }
}
