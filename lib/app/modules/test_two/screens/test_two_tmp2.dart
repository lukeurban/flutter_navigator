import 'package:flutter/material.dart';

class TestTwoTmp2 extends StatelessWidget {
  static String route = '/test-two/tmp2';
  const TestTwoTmp2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('TestTwoTmp2'),
      ),
    );
  }
}
